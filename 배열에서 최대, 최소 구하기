#include <stdio.h>

 

int main(){

    int arr[5] = {0,};

    int i=0;

    int max, min;

    for(i=0; i<5; i++){

        scanf("%d", &arr[i]);

    }

    max = arr[0], min = arr[0];

    

    for(i=0; i<5; i++){

        if(max<arr[i])

            max = arr[i];

    }

    

    for(i=0; i<5; i++){

        if(min>arr[i])

            min = arr[i];

    }

    printf("MIN: %d, MAX: %d", min, max);;

    return 0;

    }
